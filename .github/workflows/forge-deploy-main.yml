# Deployment managed by ForgeScaler via CodeBuild.
# This workflow is a manual fallback — automatic deploys happen
# through the ForgeScaler platform when PRs merge.
name: Deploy main
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is managed by ForgeScaler. Merging to main auto-deploys."
